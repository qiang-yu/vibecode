# defence-llm-server dependencies
# Verified with Python 3.12 (conda env "RQwen"). Versions below are what this
# server was tested against; loosen the pins if you need to.

# --- Core runtime (imported directly by defence-llm-server.py) ---
fastapi==0.136.3
uvicorn==0.48.0
httpx==0.28.1
transformers==4.57.6
